import style from './contactme.module.css';

export default function ContactMe() {
    return(
        <section className={`${style.contactMeSection}`}>
            <h1>ContactMe</h1>
        </section>
    )
}