import style from './certificate.module.css';
export default function Certificates() {
    return (
        <section className={`${style.certificateSection}`}>
            <h1>Certificates</h1>
        </section>
    )
}